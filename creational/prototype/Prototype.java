package creational.prototype;

public interface Prototype {
    // Note: public(access modifier) is not required since for interfaces, all their methods are public by default
    public Car clone();

}
